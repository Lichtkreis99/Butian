# 步天 BuTian — 项目简报（选题：宣夜）

用 Gaia Sky 的真实数据搭一个三维星场，把星盘、七政四余、四柱八字这三种盘面接回去：
选一个时间，右侧出盘，左侧三维视窗飞到对应的观察视角，并用「投影线」直观展示
盘上每一格是怎么从真实天体投影出来的。选题背景见
`/Users/admin/Desktop/毕业设计/选题讨论/预选方案卡/11_宣夜.md`（只读）。

## 1 硬性约束

- **交付物**：`npm run build` 生成 `dist/`，在 Chrome 和 Safari 里用 `file://` 双击
  `dist/index.html` 就能完整使用。
  - 不得用 fetch / XHR 读取本地文件；不得有外链的 `<script type="module" src>`；
    不得依赖任何网络资源（CDN、远程字体）。
  - 建议做法：Vite + `vite-plugin-singlefile` 把应用 JS/CSS 内联进 index.html，
    `base: './'`；星表等大数据做成经典脚本 `dist/data/*.js`
    （给 `window.__XUANYE_*__` 赋值，内容是 base64 编码的二进制），用普通 `<script src>` 引入。
- **技术栈**：Vite + TypeScript（strict）+ three.js，界面用原生 TS 写，不引入 React 等 UI 框架。
  依赖已在宿主机装好（见 package.json）。沙箱断网，**不能 npm install 新包**；
  确实需要时停下来，在报告里写明。
- **只读区域**：`/Users/admin/Projects/Stern/**` 和 `~/.gaiasky/**` 一律不许改动。
  需要的原始数据已复制到 `data-src/`，参考引擎已原样复制到 `refs/`。`refs/` 也不许改。
- **研究边界**：格局与神煞可作为规则知识显示，但每项只能呈现名称、类别、具体查法、
  条件、本盘依据和已知出处。不得呈现吉凶分类、倾向标志、等级、解读、断语或建议；
  原始 Aizhanxing 的 `plus` 与 `level` 字段不得进入界面。其余盘面只排结构
  （宫位、度数、相位、干支、十神、藏干、纳音等）。
- **字体**：只用系统字体，`"Songti SC", "PingFang SC", "Hiragino Sans GB", serif/sans-serif`。
- **代码风格**：2 空格缩进，每行不超过约 100 字符，按职责拆模块，不要把代码压成超长行。
- **单线程工作**：不要派生并行专家、子代理或 builder worktree。读大文件时用小脚本
  只打印需要的部分。
- **Git**：沙箱里 `.git` 是只读的，不要尝试提交；由 Claude 验收后在宿主机上提交。
- **界面语言**：简体中文为主，天体名称中英对照。

## 2 数据源（`data-src/`）

- `data-src/gaia-dr3-best/particles_000000.bin`、`metadata.bin`、`log`：Gaia Sky 数据集
  「Gaia DR3 best」，646,400 颗星，含全部 Hipparcos 星，历元 2016.0，由 Gaia Sky 的
  `OctreeLoader` 读取，二进制第 3 版格式。已知情况：
  - 大端序。文件头依次是 `int32 -1`、`int32 version = 3`、`int32 count = 646400`。
  - 文件共 54,012,094 字节，平均每条约 83.6 字节，说明记录里有变长字段（很可能是名字字符串）。
  - 第一条记录开头像是 3 个 float64（位置），后面跟若干 float32。
  - 推测 Gaia Sky 内部长度单位是 1 u = 1e9 m，坐标轴约定
    `x = r·cosδ·sinα, y = r·sinδ, z = r·cosδ·cosα`。**以上都只是推测**，
    必须用已知恒星实测核对（见第 6 节），把最终确定的格式写进 `docs/gaia-format.md`。
  - `log` 是数据集生成日志，可以从里面查字段信息。
- `data-src/skycultures/chinese/index.json`、`description.md`：Stellarium 的中国星官数据，
  共 318 个星官，连线用 HIP 编号，`common_names` 是 HIP → 中文星名。
  许可为 GPL-2.0+，界面的「关于」里要注明来源和许可。
- 行星历表：Gaia Sky 用 VSOP87；网页端用 `astronomy-engine` 2.1.19（同样基于 VSOP87），
  不需要额外数据。

## 3 参考引擎（`refs/`，只读）

| 目录 | 内容 | 来源 |
|---|---|---|
| `refs/aizhanxing-chart/` | 现代西洋星盘：宫位制、相位、岁差 | 爱占星克隆，已用原站样本校验 |
| `refs/aizhanxing-zhengyu/` | 七政四余（依赖 `@/lib/aizhanxing-chart`） | 同上 |
| `refs/aizhanxing-regions/` | 地区、经纬度、时区 | 同上 |
| `refs/almuten/` | 古典占星 `calculations.ts` + 城市表 `cities.ts` | Almuten 克隆，已校验 |
| `refs/pcbz/` | 四柱八字 `bazi.ts` 等（依赖 `lunar-javascript`） | 问真八字克隆 |

移植方式：复制到 `src/lib/` 下的对应目录，只改 import 路径，不改计算逻辑；
然后写一个一致性校验脚本，逐项比较 `src/lib` 和 `refs` 的输出。

## 4 产品（联动版）

**布局**：顶部是时间栏；左侧三维视窗约占 62%；右侧盘面栏约占 38%，可折叠；窄屏时上下排列。

**时间栏**：日期时间、时区、地点（城市搜索，用 regions 数据）、「现在」按钮、播放/暂停、
速度档（1 秒 = 1 分 / 1 时 / 1 日 / 1 月 / 1 年）。支持 1000–2500 CE，超出范围时显示精度警告。

**三维视窗（宣夜层）**：
- 画 Gaia 恒星、太阳、八大行星、冥王星、月球（带轨道线，行星用夸张尺寸的标记），加黄道面网格。
- 相机要能跨尺度平滑缩放，从地月尺度一直推到数千秒差距（用对数深度缓冲，加浮动原点或
  相机相对坐标）。支持自由飞行、绕点旋转、点击天体后飞过去聚焦。
- 恒星用自定义着色器：点的大小按星等，颜色按色指数，加法混合。

**视角预设**（切换时平滑飞行约 1.5 秒）：
1. 太阳系俯视：在黄北极上方看黄道面。
2. 地心俯视：同上，但以地球为中心，方便看投影线。盘面模式默认用这个。
3. 地球视点：站在地球上往外看，为第三期的浑天层预留。
4. 宣夜漫游：离开太阳系，看恒星的真实三维分布。
5. 他星回望：飞到选定的恒星（默认天狼星），回头看太阳。

**投影线层（本作核心画面）**：从地球出发，经过每颗七政和行星画一条线，一直延长到外圈的黄道环。
环上标出回归黄道十二宫和二十八宿（二十八宿按距星分宿，各宿度数不等；
分宿标准以 `refs/aizhanxing-zhengyu` 为准，保证三维和七政盘对得上）。
线落在环上的位置就是它在盘上的位置。悬停某一宿时，显示它的距星在 Gaia 数据里的当日实际黄经。

**信息面板**（参考 Stellarium）：点击天体后显示中英文名、HIP 号、赤经赤纬（J2000 和当日各一组）、
黄经黄纬、距离（pc / 光年 / AU）、视星等和绝对星等、色指数、当地地平高度和方位角、所属中国星官。

**盘面栏（第二期做）**：三个标签页，分别是星盘（现代/古典可切换）、七政四余、四柱八字。
- 视觉统一重新设计：深色星空底、细线、朱砂和金色点缀、宋体标题，全部用 SVG 画，
  不照搬原站界面。
- 联动：在盘上点任何一颗曜、一个宫或一个宿，三维视窗飞到对应视角，高亮对应的天体和投影线；
  在三维里点行星，盘上对应的位置高亮。
- 切换标签时自动换视角：星盘和七政用「地心俯视 + 投影线」；八字用「太阳系俯视 + 节气层」。
- **八字的天文对照层**：
  - 地球公转轨道按太阳黄经每 15° 切成 24 个节气扇区。
  - 年柱以立春（黄经 315°）为界，月柱以「节」为界，高亮当前所在的扇区。
  - 时柱画成地球自转表盘，按当地真太阳时。
  - 日柱标注「六十甲子循环计数，无天体对应」。
- **四余**：
  - 罗睺、计都是月球轨道和黄道的两个交点，在三维里画成月球轨道上的虚点
    （哪个对应升交点、哪个对应降交点，先读 refs 里的引擎确认）。
  - 月孛是月球远地点，也画成虚点。
  - 紫气标注「无物理对应的推算周期」。

**第三期（本轮不做）**：浑天层（在任意视点生成天球投影，中国星官连线随视点移动而扭曲、解体）、
盖天层平面投影、完善「盘面 ⇄ 实体」的双向对照。

## 5 数据管线

`scripts/build-stars.ts`（用 `npx tsx` 运行）：
- 解码 Gaia 二进制。
- 选星：保留全部 HIP 星，加上视星等 ≤ 8.5 的星，总数上限约 15 万颗；中国星官用到的 HIP 星必须全部保留。
- 输出 `public/data/stars.js`（经典脚本，`window.__XUANYE_STARS__ = { count, base64, ... }`）。
  每颗星记录：位置 xyz（float32，单位 pc，日心 J2000 黄道坐标）、空间速度（3 个 float32）、
  视星等、颜色（B−V 或同类指标）、HIP 号（int32，没有就填 0）。
- 另外输出 `public/data/skyculture-chinese.js`：星官连线和中文星名，只保留用得到的字段。
- `dist/` 总体积控制在 12 MB 以内。

**坐标约定**：场景世界坐标采用日心 J2000 黄道坐标系，x 轴指向 J2000 春分点，z 轴指向黄北极。
在 three.js 里可以换成 y 轴朝上，但转换只能写在一个模块里，全项目统一。「当日」黄道的岁差用
astronomy-engine 的旋转矩阵函数（如 `Rotation_EQJ_ECT`）处理。

## 6 验收

`npm run check` 必须全部通过。它依次执行 typecheck、build 和下面的校验脚本。

- **verify-stars**：解码出的记录数必须是 646400。下面几颗星的 ICRS 赤经赤纬误差不超过 0.05°，
  距离落在给定范围内：

  | 星名 | HIP | 赤经 | 赤纬 | 距离 |
  |---|---|---|---|---|
  | 天狼 | 32349 | 101.287° | −16.716° | 2.64 ± 0.05 pc |
  | 南门二 A | 71683 | 219.90° | −60.83° | 1.34 ± 0.03 pc |
  | 织女一 | 91262 | 279.23° | +38.78° | 7.68 ± 0.1 pc |
  | 北极星 | 11767 | 37.95° | +89.26° | 80–140 pc |
  | 参宿四 | 27989 | 88.79° | +7.41° | 100–250 pc |

  另外，中国星官引用的每一个 HIP 都要能在输出数据里找到，缺失数为 0；做不到就列出缺失项并说明原因。
- **verify-ephemeris**：取至少 5 个日期，投影线所用的地心视黄经与 `refs/aizhanxing-chart`
  算出的行星黄经相差小于 0.01°。
- **verify-file-protocol**：构建出的 `dist/index.html` 里没有外链 module 脚本，
  产物里没有对本地文件的 `fetch(`，`dist/` 引用到的所有文件都存在。
- **第二期追加**：
  - parity-engines：随机取 50 组时间和地点，比较 `src/lib` 移植版和 `refs` 的输出，
    度数误差小于 1e-6，文字字段完全一致。
  - 八字回归样例：1990-05-15 08:00 → 庚午 辛巳 庚辰 庚辰。
- **所有期**：`src/` 下不许嵌入或回放任何样本返回值。

## 第三期实现说明

第三期已实现宣夜、浑天、盖天三态联动。Stellarium EPH 星表补回了 Gaia 数据缺少的 38 个
星官 HIP：32 颗采用正视差距离，6 颗距离未知并在宣夜层按 1000 pc 空心示意；星官线表不再
丢弃顶点。EPH 结构记录于 `docs/stellarium-eph-format.md`。

浑天层使用恒星和星官线共用的着色器投影参数，在约 1.5 秒内从真实三维位置变形到以指定机位
为心的固定半径天球。机位支持地球、太阳、当前相机和所选恒星，并提供锁定、深度连线、三类
星官开关、星官成员/深度跨度信息及可暂停重播的参宿方向解体演示。

太阳系采用真实半径与约 4 px 的 LOD 交叉淡化。太阳使用临边昏暗着色器和柔光，太阳点光源形成
行星与月球明暗界线；各天体用 `RotationAxis` 设置自转轴和转角。低清 Gaia Sky 纹理通过经典
脚本内的 data URI 首次按需加载；地球用六面日景、云层和暗面夜光，土星环位于赤道面。没有增加
后处理通道，新增实体不超过 16 次绘制。

右栏新增盖天图：以当日北天极为中心的等距方位 SVG 投影，显示内规、赤道、外规、黄道、二十八
宿距星辐射线，以及星官线、星等点和三垣/二十八宿标签。宿和星官可与三维层双向选择。

验收新增 `verify-projection` 与 `verify-textures`，并将 14 MiB 成品上限纳入检查。实现偏差是：
距离未知的 6 颗 EPH 记录没有可用视差，故仅保证方向；受无浏览器验收约束，本期只执行类型检查、
构建和脚本化数学/数据/file 协议验证。

## 第四期实现说明

第四期将右栏扩展为观星、星盘、七政四余、四柱八字、盖天图五种上下文，并以单一配置表控制
各上下文涉及的天体、黄道环、二十八宿、投影线、四余、节气与星官强调状态。设置采用版本化
本地存储和订阅接口；无存储值或 file:// 隐私限制下均回退到默认值。名称表统一服务三维标签、
信息卡和盘面文字模式，恒星当代名从 Gaia 的 Bayer 别名转换为中文星座名与希腊字母。

工作区新增可用指针和键盘操作的竖向分隔条，双击恢复 62/38，窄屏自动隐藏。渲染器、相机和
所有 DOM 标签都跟随视窗 ResizeObserver 更新，折叠盘面不再改变三维画面的纵横比。

太阳系远距 LOD 由实色标记改为按相机视星等缩放的加法辉光星点；近距纹理球保持不变。星盘
和七政盘新增可记忆的文字/符号模式。同心圆对照在约 1.2 秒内把涉及天体和三个位置信息明确的
四余点移至共同盘面半径，纬度归零，轨道淡出且投影线收为径向；计算与信息卡仍使用真实位置。

星盘角度改为 ASC 固定左侧、黄经逆时针，并从黄北极方向匹配。七政原图的黄经方向相对黄北极
视图为镜像，因此没有翻转历算数据，而是明确从黄南极方向匹配。

浑天默认进入天象仪内视：观测点来自时间栏地点，拖拽只改变方位和高度，滚轮视场限制为
1°–180°。恒星、星官线、行星、网格与标签共享球极/透视/等距鱼眼映射；地面、地平线、八方标、
大气和地平/赤道/黄道/子午线可配置。机位可切换到所选恒星，此时自动关闭地面与大气。外视
保留第三期的机位锁定、深度连线和解体演示。星盘轴点与宫头、七政和盖天宿界可转动内视并
高亮天空目标，信息卡增加地平坐标及升起、中天、落下时刻。

验收新增 verify-context、verify-names、verify-virtual-radius、verify-orientation 和
verify-planetarium；成品体积上限调整为 15 MiB。受无浏览器约束，视觉行为仍以类型检查、构建、
数学与数据回归及 file:// 协议检查验证。

## 第五期实现说明

第五期修正工作区收缩链：视窗、宿主与画布都允许在网格中收缩，画布绝对铺满宿主，渲染器只
更新绘图缓冲而不写内联样式。相机、DOM 标签和叠加层使用同一内容框，并提供布局浏览器自测。

浑天内视的地面与大气改为逐像素全屏反投影，球极、透视和等距鱼眼都不再依赖跨大角度三角形。
底部快捷栏连接同一设置存储，可在中国星官和 88 个 IAU 西方星座间切换。西方连线与中文 IAU
名称来自 Stellarium skyculture；85 幅 GPL v2.0 插图由三个 HIP 锚点解算并以 16×16 网格变形。

Guéreins 地景打包 nested HEALPix Norder1，并以 Norder0 作解码回退。其水平坐标约定为北极指向
天顶、经度零点指向南、正向朝西；网格顶点经过当前天象仪投影，透明天空保留，并随太阳高度
在夜间压暗。具体格式见 `docs/landscape-format.md`。

盘面符号不再依赖字体或彩色表情字符：十二宫、行星与交点均使用等线宽 SVG 路径。四个比较
盘面共享「隐藏未涉及天体」设置，规则集中在上下文配置表；恒星成员和距星标志在初始化时写入
GPU 属性，切换时只改 uniform。行星可见度按当前中心天体距离平滑淡出；恒星距离双滑块支持
1–5000 pc 与无限范围，未知距离六星仅在完整范围显示，星官线任一端隐藏时整段隐藏。

验收新增 verify-layout、verify-culture、verify-glyphs 和 verify-visibility，并扩展
verify-planetarium 覆盖三种投影的 30,000 次往返、地面分类与 nested HEALPix 中心往返。
成品体积上限调整为 18 MiB；浏览器行为由 `#selftest=layout` 与 `#selftest=ground` 写入
`document.documentElement.dataset.selftest` 供宿主验收。

## 第七期实现说明

第七期把交互重新收进固定区域：顶部只放时间地点、模型切换、设置与关于；左上视点坞在宣夜
显示五种预设，在浑天显示机位和内外视控制；底部图层栏适用于全部模型，显示范围抽屉固定于
左下，信息卡固定于右下并独立滚动。右栏采用标签、可换行盘面工具栏、盘面和可折叠表格的顺序，
隐藏未涉及天体不再与宫制重叠。设置页仅保留低频偏好，所有快捷开关仍订阅同一设置存储。

七政四余工具栏按原始设置定义列出黄道制、宿制、命宫、身宫及交点。当前可计算回归黄道今宿
与恒星黄道今宿，其余原始体系标为「未实现」；恒星黄道选择同时驱动七政盘、三维二十八宿环、
投影线及同心圆对照。联动验证覆盖两种 calc_type、五个日期和七曜的黄经与宿序。

盖天图的恒星半径改用按星等限制在 0.45–2.2 的函数，非星官星以 5.5 等为截止。浑天的地平
网格和赤道网格都补全至下半球；地面以下的线仍绘于地景之上，但透明度降为 40%，地平线最后
绘制。拖拽测试固定在北京夜间，并用天象仪投影域、地平线和可见性规则计算内视恒星期望值。

`#selftest=ui` 扩展为四种天空状态、五个标签、三种盘宽和显示范围抽屉开闭的组合测试，支持
按 `mode` 分块；它检查四个固定坞、盘面工具控件、视窗边界和文字溢出。新增纯测试
verify-gaitian 与 verify-grid，verify-linkage 增加 calc_type 4 覆盖；成品仍遵守 18 MiB 与
file:// 限制。

## 第八期实现说明

第八期只在右侧盘面加入结构关系，并保留左侧三维场景原状。星盘圆盘按相位类别画连线，盘下
增加三角相位格和含实角、容许度、入相/出相状态的逐项表；现代相位容许度可在设置中调整，
古典模式继续采用 Almuten 引擎的托勒密相位及光体半径。点击相位会同时高亮两个天体。

七政四余把 makeYaos 的本命、流年结果逐项转换为年柱、日柱或命宫首宫的具体查法。格局目录
来自 26 份只读 API 样本的 132 个去重名称、分组和条件注记，不复制样本命中结果；80 项已有
基于宫主、曜位、垣殿、宿躔、同宫或对宫关系的谓词，52 项显示「规则待考」。样本回归总体
precision 44.3%、recall 37.2%，验证下限为 35% recall；分歧规则由 verify-geju 完整列出。
其中「寒月单行」的来源注记含倾向性后半句，界面依研究边界只显示其条件部分。

四柱八字在四柱表下加入干支关系 SVG，以合、冲、刑、害、破、克、会等关系类型中性配色，
并允许点击关系或神煞行高亮涉及的干支。神煞查法直接复用 computeShenSha 的日干、年日支、
月支、日柱等查表基准，不显示原数据中的倾向或等级字段。

新增 verify-aspects、verify-yaos-rules、verify-geju 与 verify-relations；边界检查改为允许规则
知识并拒绝分类、解释性或建议性渲染文本。`#selftest=charts` 增加新章节存在性、相邻区块重叠、
横向边界和同步盘面渲染预算检查，`#selftest=ui` 在原 120 种组合中同步检查这些章节。

## 第九期实现说明

浑天内视采用按视场和 Bortle 等级变化的极限星等。恒星点大小由相对尺度、绝对尺度和星等共同
决定，只给亮星保留柔光；大气开启时按约 0.2 等/airmass 做消光，低空轻微红化，并使用
Saemundsson 公式修正近地平折射。标签星等阈值和总预算随视场变化，宽视场最多 25 项，星官名
使用成员方向的球面质心。默认关闭网格。

新增 Stellarium Milky Way Norder0 的 12 张赤道坐标 HEALPix 纹理，并以网格逐顶点通过现有三种
天象仪投影；亮度随 Bortle、日月高度、大气和视场变化。Stellarium Mobile DSO EPH 的 Norder0
共解出 9,032 项，以一次 GPU 点绘制按星系、疏散/球状星团和星云显示中性标记；Messier/NGC
编号可搜索，点击显示类别、坐标、星等、尺寸与形态。EPH 的列和字节平面转置见
`docs/stellarium-eph-format.md`。

内视拖拽增加指数衰减惯性，约 60 帧降至 1% 以下；缩放在对数视场插值并向光标方向偏移。
搜索支持中文、英文、HIP、Bayer、行星、DSO 编号和星官，空格回中、T 跟踪，方向键与
PageUp/PageDown、加减号控制视角，J/K/L/8 控制时间。底部时间栏与顶部历元双向同步，和图层栏
一样在三秒无边缘活动后自动隐藏。

设置新增 Bortle 1–9、恒星相对/绝对尺度及银河、DSO 默认开关；关闭大气同时停用消光和天空
辉光。新增 verify-sky 覆盖光污染/视场函数、消光折射、惯性、缩放、搜索、银河数据和 M42 EPH
解码；`#selftest=sky` 可按七个 part 分块。成品上限调整为 20 MiB，仍保持纯 `file://` 数据加载。
